/**
 * deepseek-harness-zh_pro 浏览器半边源码说明。
 *
 * 客户端业务片段均为 TypeScript 脚本，按 scripts/build-client.mjs 中的固定顺序
 * 合并到一个经典脚本工厂。这里保留插件行为、语言边界与构建约束的总说明；
 * __ModuleLoader__ 包壳由构建器生成，避免单个片段依赖不完整的语法上下文。
 *
 * 中文补全只在中文界面和 zhComplete 开启时改写 locale 与有限 DOM 清单；统计全显示、
 * 思考展开、默认展开行数、对话宽度、自动归档、会话删除按钮和归档会话视图按各自
 * 设置独立生效。「删除会话（回收站）」菜单项在中文和英文界面都注入（文案随界面语言
 * 切换），经主机 /dsh-zh/api 路由把日志移入系统回收站并移除工作区账本槽位；「归档」
 * 按钮同样中英文界面都注入，点击后把归档会话行以纯 DOM 容器注入官方列表该工作区
 * 分组末尾（不使用 React 槽位，随官方列表整体滚动），点击归档行即静默取消归档
 * （恢复）并打开该会话。「服务监控」在中英文界面都生效：主机半边扫描本机监听
 * 端口（基线 = 插件启动时已在监听的端口），浏览器轮询 /dsh-zh/api/service-monitor
 * 快照并在左侧会话列表与设置之间注入绿点面板，点击条目在新标签页打开该服务。
 * 提示词注入由主机 settingsScope 控制，默认关闭。
 * 所有副作用都必须随当前插件 Fiber 清理。
 */
